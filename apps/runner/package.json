{
  "name": "@blackglass/runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Blackglass native host runner — nmap execution slice for M4",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blackglass/contracts": "workspace:*",
    "@blackglass/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
